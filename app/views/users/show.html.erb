<% provide(:title, @user.name) %>
<div class="row">
  <aside class="span4">
    <section>
      <h1>
        <%= gravatar_for @user %>
        <%= @user.name %>
      </h1>
    </section>
  </aside>
  <div class="span8">
    <% if @user.the_microposts.any? %>
      <h3>Microposts (<%= @user.the_microposts.count %>)</h3>
      <ol class="microposts">
        <%= render @the_microposts %>
      </ol>
      <%= will_paginate @the_microposts %>
    <% end %>
  </div>
</div>